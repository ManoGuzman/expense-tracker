name: Expense Tracker CI

on:
  push:
    branches: [master, feature-*]
  pull_request:
    branches: [master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Add Spotless plugin
        shell: bash
        run: |
          if ! grep -q "id 'com.diffplug.spotless'" build.gradle; then
            echo "plugins { id 'com.diffplug.spotless' version '6.19.0' }" >> build.gradle
            echo "" >> build.gradle
            echo "spotless {" >> build.gradle
            echo "    java {" >> build.gradle
            echo "        googleJavaFormat()" >> build.gradle
            echo "    }" >> build.gradle
            echo "}" >> build.gradle
          fi

      - name: Run Spotless check
        run: ./gradlew spotlessCheck

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Check for test files
        id: has_tests
        shell: bash
        run: |
          if [ -d "src/test/java" ] && [ "$(ls -A src/test/java 2>/dev/null)" ]; then
            echo "has_tests=true" >> $GITHUB_OUTPUT
          else
            echo "has_tests=false" >> $GITHUB_OUTPUT
          fi

      - name: Run tests
        if: steps.has_tests.outputs.has_tests == 'true'
        run: ./gradlew test || true

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Build
        run: ./gradlew build
